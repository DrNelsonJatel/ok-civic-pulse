project:
  type: website
  output-dir: ../docs      # static output; free to host on Vercel / GH Pages

website:
  title: "Okanagan Civic Pulse"
  description: >
    Which issues in Okanagan public discourse a local government can actually
    act on — ahead of the BC general local election, 2026-10-17.
  page-footer:
    left: "Limnology Research · personal research project"
    right: "Source: Castanet public forums. Aggregate counts only — no comment text, no handles."

format:
  dashboard:
    theme: [cosmo, theme.scss]
    nav-buttons: []
    expandable: false

# GitHub Pages runs Jekyll on the published folder unless .nojekyll exists,
# and Jekyll drops anything beginning with an underscore. Quarto clears the
# output dir on render, so this must be recreated each time.
post-render:
  - bash -c "touch ../docs/.nojekyll"

execute:
  echo: false
  warning: false
  freeze: false
